import { h } from "preact";
import cx from "classnames";

import "./content.css";

const BASE_CLASS = "content";

const Content = props => {
  return <div className={BASE_CLASS}>Hello World!</div>;
};

export default Content;
