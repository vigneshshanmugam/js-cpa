import { h } from "preact";
import { Layout } from "preact-mdl";

export default () =>
  <Layout.Header>
    <Layout.HeaderRow>
      <Layout.Title>Hello CPA</Layout.Title>
    </Layout.HeaderRow>
  </Layout.Header>;
